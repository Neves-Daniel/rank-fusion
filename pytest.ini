[pytest]
# Permite `from src.data import ...` rodando pytest da raiz do projeto.
pythonpath = .
testpaths = tests
addopts = -q
markers =
    bm25: usa o retriv/BM25 real (mais lento; pulado se retriv não estiver instalado)
