# Download
gdown>=5.0.0

# Recuperação esparsa (BM25) — metodologia xCoRetriev / RAG-Fuse
retriv>=0.2.3            # BM25 + tokenização; traz ranx, torch e transformers junto
# IMPORTANTE: após instalar, baixe os recursos do NLTK exigidos pelo retriv:
#   python -m nltk.downloader punkt punkt_tab stopwords
# (bm25s foi descartado: usamos retriv para fidelidade ao código do autor.)

# Recuperação densa — bi-encoder BERT fine-tuned (label-as-document) + RAG-labels (ver TECH_STACK.md)
# Porte do DenseRetriever do RAG-Fuse em torch+transformers puros (já vêm via retriv).
# A loss NT-Xent + miner por relevance-map é fiel ao RAG-Fuse via:
pytorch-metric-learning>=2.0    # losses.NTXentLoss + DotProductDistance + RelevanceMiner (BaseMiner)
# (sem sentence-transformers / faiss: encoder e similaridade doc×rótulo são exatos,
#  feitos à mão — ConcatenatePooling 3072-d + matmul cosine sobre ~4k rótulos.)
# Geração das RAG-labels (descrições de rótulo via LLM) — SÓ na Brev/GPU, NÃO na WSL local.
# Import é lazy em src/label_desc.py (VLLMBackend); a suíte mínima de testes não exige vllm.
# vllm traz seu próprio torch/CUDA — instalar na Brev conforme a GPU disponível.
vllm                           # serve meta-llama/Llama-3.1-8B-Instruct localmente

# Fusão / normalização / métricas clássicas
ranx>=0.3.20            # (já vem como dependência do retriv)

# Métricas de XMTC (PSP@k / PSnDCG@k)
# O repo se chama pyxclib, mas o pacote Python instalado é "xclib".
xclib @ git+https://github.com/kunaldahiya/pyxclib.git

# Utilitários
# numpy fixado em 1.x: o xclib é compilado contra o ABI do numpy 1.26 e o
# faiss-cpu<1.9 também espera numpy 1.x. Subir para 2.x quebraria os dois.
numpy<2
scipy
tqdm
